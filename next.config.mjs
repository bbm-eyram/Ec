/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add your allowed image domain here
    },
  };
  
  export default nextConfig;  